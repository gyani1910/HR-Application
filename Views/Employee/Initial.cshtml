@* @model NewApp1.Models.loginViewModel
@{
    ViewData["Title"] = "Login";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E2E2E2; /* Light Gray background */
            color: #2C2C54; /* Dark Indigo text color */
            margin: 0;
            padding: 0;
        }
        .navbar {
            background-color: #2C2C54; /* Dark Indigo for navbar */
        }
        .navbar-brand, .navbar-nav .nav-link {
            color: #ECECEC !important; /* Light Gray text color for navbar */
        }
        .navbar-nav .nav-link:hover {
            color: #AAABB8 !important; /* Cool Gray on hover */
        }
        .container {
            position: relative;
            max-width: 500px;
            margin: 50px auto;
            height: 500px; /* Increased height to accommodate forms */
            overflow: hidden; /* Hide overflow */
            padding: 0; /* Remove padding */
        }
        .form-wrapper {
            display: flex;
            transition: transform 0.5s ease-in-out;
            height: 100%;
            width: 200%; /* Ensure enough space for sliding */
            position: absolute;
            top: 0; /* Align top */
            left: 0; /* Align left */
            overflow: hidden; /* Hide overflow */
        }
        .form-box {
            width: 50%; /* Each form takes up half the width */
            background-color: #2C2C54; /* Dark Indigo for form boxes */
            color: #ECECEC; /* Light Gray text color */
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.5); /* Visible shadow effect */
        }
        .form-box h2 {
            color: #ECECEC; /* Light Gray text color for headers */
        }
        .form-wrapper.hidden {
            transform: translateX(0%);
        }
        .form-wrapper.register-active {
            transform: translateX(-50%);
        }
        .form-control {
            background-color: #ECECEC; /* Light Gray for input fields */
            border-color: #2C2C54; /* Dark Indigo border color */
            color: #2C2C54; /* Dark Indigo text color for inputs */
        }
        .form-control::placeholder {
            color: #2C2C54; /* Dark Indigo placeholder text color */
        }
        .btn-primary {
            background-color: #FFFFFF; /* Dark Indigo button color */
            border-color: #2C2C54; /* Border color for primary button */
        }
        .btn-primary:hover {
            background-color: #ECECEC; /* Deep Purple-Gray on hover */
            border-color: #2C2C54; /* Border color on hover */
        }
        .alert {
            background-color: #FF4D4D; /* Red background for alerts */
            color: #ECECEC; /* Light Gray text color */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#" id="show-login">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" id="show-register">Register</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="form-wrapper hidden">
            <div class="form-box login-form">
                <h2>@ViewData["Title"]</h2>
                @if (ViewBag.error != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <strong>Error!</strong> @ViewBag.error
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                }
                <form asp-action="Initial" method="post">
                    <div class="form-group">
                        <label asp-for="Username" class="control-label"></label>
                        <input asp-for="Username" class="form-control" placeholder="Username" />
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control" type="password" placeholder="Password" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Login" class="btn btn-primary" />
                    </div>
                </form>
            </div>

            <div class="form-box register-form">
                <h2>Register</h2>
                <form asp-action="Register" method="post">
                    <div class="form-group">
                        <label asp-for="Username" class="control-label"></label>
                        <input asp-for="Username" class="form-control" placeholder="Username" />
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control" type="password" placeholder="Password" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Register" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.11.8/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#show-register').click(function () {
                $('.form-wrapper').removeClass('hidden').addClass('register-active');
            });
            $('#show-login').click(function () {
                $('.form-wrapper').removeClass('register-active').addClass('hidden');
            });
        });
    </script>
</body>
</html> *@

@model NewApp1.Models.loginViewModel
@{
    ViewData["Title"] = "Login";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E2E2E2; /* Light Gray background */
            color: #2C2C54; /* Dark Indigo text color */
            margin: 0;
            padding: 0;
        }
        .navbar {
            background-color: #2C2C54; /* Dark Indigo for navbar */
        }
        .navbar-brand, .navbar-nav .nav-link {
            color: #ECECEC !important; /* Light Gray text color for navbar */
        }
        .navbar-nav .nav-link:hover {
            color: #AAABB8 !important; /* Cool Gray on hover */
        }
        .container {
            position: relative;
            max-width: 500px;
            margin: 50px auto;
            height: 500px; /* Increased height to accommodate forms */
            overflow: hidden; /* Hide overflow */
            padding: 0; /* Remove padding */
        }
        .form-wrapper {
            display: flex;
            transition: transform 0.5s ease-in-out;
            height: 100%;
            width: 200%; /* Ensure enough space for sliding */
            position: absolute;
            top: 0; /* Align top */
            left: 0; /* Align left */
            overflow: hidden; /* Hide overflow */
        }
        .form-box {
            width: 50%; /* Each form takes up half the width */
            background-color: #2C2C54; /* Dark Indigo for form boxes */
            color: #ECECEC; /* Light Gray text color */
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.5); /* Visible shadow effect */
        }
        .form-box h2 {
            color: #ECECEC; /* Light Gray text color for headers */
        }
        .form-wrapper.hidden {
            transform: translateX(0%);
        }
        .form-wrapper.register-active {
            transform: translateX(-50%);
        }
        .form-control {
            background-color: #ECECEC; /* Light Gray for input fields */
            border-color: #2C2C54; /* Dark Indigo border color */
            color: #2C2C54; /* Dark Indigo text color for inputs */
        }
        .form-control::placeholder {
            color: #2C2C54; /* Dark Indigo placeholder text color */
        }
        .btn-primary {
            background-color: #B0B0B0; /* Grey background for better visibility */
            border-color: #2C2C54; /* Dark Indigo border color */
            color: #2C2C54; /* Dark Indigo text color */
        }
        .btn-primary:hover {
            background-color: #A0A0A0; /* Darker Grey on hover */
            border-color: #2C2C54; /* Dark Indigo border on hover */
            color: #2C2C54; /* Dark Indigo text color on hover */
        }
        .alert {
            background-color: #FF4D4D; /* Red background for alerts */
            color: #ECECEC; /* Light Gray text color */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#" id="show-login">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" id="show-register">Register</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="form-wrapper hidden">
            <div class="form-box login-form">
                <h2>@ViewData["Title"]</h2>
                @if (ViewBag.error != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <strong>Error!</strong> @ViewBag.error
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                }
                <form asp-action="Initial" method="post">
                    <div class="form-group">
                        <label asp-for="Username" class="control-label"></label>
                        <input asp-for="Username" class="form-control" placeholder="Username" />
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control" type="password" placeholder="Password" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Login" class="btn btn-primary" />
                    </div>
                </form>
            </div>

            <div class="form-box register-form">
                <h2>Register</h2>
                <form asp-action="Register" method="post">
                    <div class="form-group">
                        <label asp-for="Username" class="control-label"></label>
                        <input asp-for="Username" class="form-control" placeholder="Username" />
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control" type="password" placeholder="Password" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Register" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.11.8/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#show-register').click(function () {
                $('.form-wrapper').removeClass('hidden').addClass('register-active');
            });
            $('#show-login').click(function () {
                $('.form-wrapper').removeClass('register-active').addClass('hidden');
            });
        });
    </script>
</body>
</html>
